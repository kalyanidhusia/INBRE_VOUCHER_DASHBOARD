.rscignore
app_jun22.R.bak
renv.lock
scripts_clean_core_requests.R
scripts_join_core_outcomes.R
working_app.R.June22

raw_data/
*.csv
# R artifacts
.Rhistory
.RData
.Rproj.user/
*.Rproj

# renv local library (not needed in repo — lockfile is enough)
renv/library/
renv/staging/

# shinyapps.io deploy cache
rsconnect/

# OS
.DS_Store
Thumbs.db

# Sensitive data overrides (lab members upload via the UI; don't commit real exports)
# To allow the placeholder defaults, raw_data/*.csv IS tracked.
# If you add a real export, name it differently or add it here:
# raw_data/real_*.csv
